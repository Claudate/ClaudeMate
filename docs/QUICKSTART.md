# 🚀 Claudate - 快速开始指南

5分钟快速上手 Claudate（Claude Skills Electron App）！

## 📋 前置要求

在开始之前，请确保已安装：

- **Node.js** 18+ LTS ([下载](https://nodejs.org/))
- **npm** 或 **yarn** 包管理器
- **Git** (可选，用于版本控制)
- **VSCode** (推荐，提供最佳开发体验)

验证安装：
```bash
node --version  # 应显示 v18.x.x 或更高
npm --version   # 应显示 9.x.x 或更高
```

---

## 🚀 快速启动

### 方法一：使用批处理脚本（Windows，推荐）

1. **双击启动脚本**
   ```
   H:\Electron\claude-skills-app\dev.bat
   ```

2. **等待应用窗口打开**（大约 5-10 秒）

### 方法二：使用命令行

```bash
# 1. 进入项目目录
cd H:\Electron\claude-skills-app

# 2. 安装依赖（首次运行）
npm install

# 3. 启动开发服务器
npm run dev
```

---

## 🎯 首次启动

### 应该看到什么？

启动成功后，您会看到：

```
┌─────────────────────────────────────────────────────┐
│  Claudate                    ─  ▭  ✕  (标题栏)    │
├──┬──────────────────────────────────────────────────┤
│💬│  Welcome to Claudate!                           │
│📁│                                                  │
│📂│  Start chatting with Claude AI...               │
│🕐│                                                  │
│🔄│                                                  │
│⚙ │                                                  │
├──┴──────────────────────────────────────────────────┤
│  No Project  •  Memory: 256 MB  •  win32  •  v1.0  │
└─────────────────────────────────────────────────────┘
```

### 基础功能测试

1. **测试侧边栏导航**
   - 点击左侧图标切换模块
   - 💬 Assistant（AI 聊天）
   - 📁 Projects（项目管理）
   - 📂 Explorer（文件浏览）
   - 🕐 History（聊天历史）
   - 🔄 Workflow（工作流）
   - ⚙️ Settings（设置）

2. **测试主题切换**
   - 点击 **⚙️ Settings**
   - 切换 Dark/Light/Auto 主题
   - 观察界面平滑过渡

3. **测试状态栏**
   - 查看底部状态栏
   - 显示当前项目、内存使用、系统信息

---

## 💬 测试 AI 聊天（Assistant 模块）

### 前提条件

需要安装 Claude CLI：

```bash
# 检查是否已安装
claude --version

# 如果未安装，执行以下命令
npm install -g @anthropic-ai/claude-code

# 登录（如需要）
claude auth login
```

### 开始聊天

1. 点击侧边栏 **💬 Assistant**
2. 在输入框输入消息：
   ```
   Hello, Claude! Can you help me understand this project?
   ```
3. 按 **Enter** 或点击 **Send** 发送
4. 观察流式响应（逐字显示）

### 快捷键

- **Enter**: 发送消息
- **Shift + Enter**: 换行
- **Ctrl/Cmd + K**: 清空聊天（如已实现）

---

## 🔧 开发工作流

### 热重载（HMR）

编辑任何前端文件后，应用会自动刷新（~200ms）：

```bash
# 试试编辑这个文件
src/renderer/App.tsx
src/renderer/components/layout/Sidebar.tsx
src/renderer/styles/index.css
```

保存后立即在应用中看到变化！

### 调试工具

**打开 DevTools**:
- Windows/Linux: `F12` 或 `Ctrl + Shift + I`
- macOS: `Cmd + Option + I`

查看：
- **Console**: 日志和错误
- **Elements**: DOM 结构
- **Network**: 请求监控
- **Sources**: 调试 TypeScript
- **Performance**: 性能分析
- **Memory**: 内存泄漏检测

### 类型检查

```bash
# 运行 TypeScript 类型检查
npm run type-check

# 监听模式（自动检查）
npx tsc --noEmit --watch
```

### 代码检查和格式化

```bash
# 检查代码风格
npm run lint

# 自动修复问题
npm run lint:fix
```

---

## 📦 构建和打包

### 构建开发版本

```bash
npm run build
```

生成文件位于 `dist/` 目录：
```
dist/
├── main/       # Electron 主进程
└── renderer/   # React 渲染进程
```

构建时间：约 30-60 秒

### 打包为可执行文件

```bash
npm run package
```

或使用批处理脚本：
```bash
package-app.bat
```

生成文件位于 `release/` 目录：
```
release/
├── win-unpacked/           # Windows 便携版
├── Claudate-Setup.exe      # Windows 安装程序
├── mac/                    # macOS 应用
└── linux/                  # Linux 包
```

打包时间：约 2-5 分钟

---

## 🐛 常见问题

### 问题 1: 端口 5173 被占用

**错误信息**:
```
Error: Port 5173 is already in use
```

**解决方案**:
```bash
# Windows
netstat -ano | findstr :5173
taskkill /PID <进程ID> /F

# 或使用 PowerShell（管理员）
Get-NetTCPConnection -LocalPort 5173 | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force }
```

### 问题 2: npm install 失败

**解决方案**:
```bash
# 清理缓存
npm cache clean --force

# 删除 node_modules
rm -rf node_modules
rm package-lock.json

# 重新安装
npm install
```

### 问题 3: Electron 窗口不显示

**可能原因**:
- 主进程崩溃
- 渲染进程加载失败

**解决方案**:
```bash
# 清理构建缓存
rm -rf dist
rm -rf node_modules/.vite

# 重新启动
npm run dev
```

查看终端输出的错误信息进行排查。

### 问题 4: TypeScript 报错

**解决方案**:
```bash
# 重新安装 TypeScript
npm install --save-dev typescript@5.3.3

# 检查配置
cat tsconfig.json

# 重启 VS Code TypeScript 服务器
# Ctrl/Cmd + Shift + P → "TypeScript: Restart TS Server"
```

### 问题 5: Claude CLI 不工作

**错误信息**:
```
Error: Claude CLI not found
```

**解决方案**:
```bash
# 全局安装 Claude CLI
npm install -g @anthropic-ai/claude-code

# 验证安装
claude --version

# 如需登录
claude auth login
```

---

## 📚 下一步学习

### 新手推荐

1. **[环境设置](./SETUP.md)** - 配置开发工具和环境
2. **[项目概览](./README.md)** - 了解所有功能特性
3. **[架构设计](./ARCHITECTURE.md)** - 理解系统设计

### 进阶开发

4. **[实现指南](./IMPLEMENTATION_GUIDE.md)** - 学习如何添加新功能
5. **[Claude CLI 参考](./docs/CLAUDE_CLI_REFERENCE.md)** - AI 集成详解
6. **[前端集成指南](./docs/FRONTEND_INTEGRATION_GUIDE.md)** - 前端开发技巧

---

## ✅ 快速开始检查清单

完成以下检查，确保一切正常：

- [ ] Node.js 18+ 已安装
- [ ] 运行 `npm install` 成功
- [ ] 运行 `npm run dev` 成功
- [ ] 应用窗口正常打开
- [ ] 可以在模块间导航
- [ ] 主题切换正常工作
- [ ] DevTools 可以打开（F12）
- [ ] 状态栏显示系统信息
- [ ] `npm run type-check` 无错误
- [ ] `npm run lint` 无错误

如果所有项都 ✅，恭喜您！可以开始开发了！

---

## 💡 开发技巧

### 推荐的 VS Code 扩展

在 VS Code 中安装以下扩展以获得最佳体验：

- **ESLint** - 代码检查
- **Prettier** - 代码格式化
- **TypeScript and JavaScript Language Features** - 智能提示
- **Tailwind CSS IntelliSense** - CSS 智能提示
- **Error Lens** - 行内错误显示

### 快速命令

```bash
# 开发
npm run dev          # 启动开发服务器

# 检查
npm run type-check   # TypeScript 类型检查
npm run lint         # ESLint 检查
npm run lint:fix     # 自动修复

# 构建
npm run build        # 构建项目
npm run package      # 打包应用

# 测试
npm test             # 运行测试（如已配置）
```

### 项目结构速查

```
src/
├── main/                # Electron 主进程
│   ├── index.ts        # 入口文件
│   ├── managers/       # 管理器
│   ├── monitors/       # 监控器
│   └── services/       # 服务
├── renderer/           # React 渲染进程
│   ├── App.tsx         # 主应用
│   ├── modules/        # 功能模块
│   ├── components/     # UI 组件
│   ├── stores/         # 状态管理
│   └── hooks/          # 自定义 Hooks
└── shared/             # 共享代码
    └── types/          # 类型定义
```

---

## 🆘 获取帮助

如果遇到问题：

1. 查看本文档的[常见问题](#🐛-常见问题)部分
2. 查看项目 [README.md](./README.md)
3. 查看 [环境设置文档](./SETUP.md)
4. 检查终端和 DevTools 的错误信息
5. 在 GitHub 提交 Issue

---

**祝您开发顺利！🎉**

---

**最后更新**: 2025-11-11
**版本**: 1.0.0
**维护**: Claude Skills Team
